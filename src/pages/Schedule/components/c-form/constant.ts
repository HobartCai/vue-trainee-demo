/**
 * 默认标签位置
 */
export const DEFAULT_LABEL_PLACEMENT = "top";
/**
 * 默认栅格占据列数，基于 24 栅格
 */
export const DEFAULT_ITEM_SPAN = 1;
/**
 * 默认栅格偏移列数，基于 24 栅格
 */
export const DEFAULT_ITEM_OFFSET = 0;
/**
 * 默认 Action 按钮位置
 */
export const DEFAULT_ACTION_JUSTIFY = "end";
/**
 * 默认输入组件 size
 */
export const DEFAULT_COMPONENT_SIZE = "medium";
